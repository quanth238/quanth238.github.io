<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://quanth238.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://quanth238.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-19T19:29:31+00:00</updated><id>https://quanth238.github.io/feed.xml</id><title type="html">blank</title><subtitle>Portfolio and academic homepage for Quan Tran Hong, a Computer Science graduate student working on 3D vision, generative modeling, optimization, neural rendering, and probabilistic machine learning. </subtitle><entry><title type="html">Flow Matching: Velocity Fields</title><link href="https://quanth238.github.io/blog/2026/flow-matching-guide-part-1/" rel="alternate" type="text/html" title="Flow Matching: Velocity Fields"/><published>2026-05-19T00:00:00+00:00</published><updated>2026-05-19T00:00:00+00:00</updated><id>https://quanth238.github.io/blog/2026/flow-matching-guide-part-1</id><content type="html" xml:base="https://quanth238.github.io/blog/2026/flow-matching-guide-part-1/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>Flow matching is a generative modeling framework built around a velocity field. Instead of learning a denoising step for a fixed noise schedule, the model learns how points should move along a continuous path from a source distribution to the data distribution. Lipman et al. introduced flow matching as simulation-free regression for continuous normalizing flows <a href="https://arxiv.org/abs/2210.02747">Flow Matching for Generative Modeling</a>. I use <a href="https://arxiv.org/abs/2412.06264">Flow Matching Guide and Code</a> as the reading spine here, but the first thing to make concrete is smaller: what path do we choose, what velocity do we regress, and how does that become a sampler?</p> <p>The object we train is $v_\theta(x, t)$, a neural velocity field. During sampling, start from $x_0 \sim p_0$ and integrate the learned field until $t=1$. If the field points in the right direction along the path, the final sample should look like data.</p> <figure> <picture> <img src="/assets/img/blog/flow-matching-guide/flow-matching-overview.svg" class="img-fluid rounded z-depth-1" width="1600" height="900" alt="Flow matching schematic showing source and target samples, interpolation times, velocity target arrows, a learned vector field, and one ODE sampling trajectory." data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>The useful sequence is short: choose a path, compute its velocity target, train with a regression loss, then sample by integrating the learned field.</p> <h2 id="problem-setup">Problem setup</h2> <p>Let $p_0$ be an easy source distribution, usually a standard Gaussian, and let $p_1$ be the target data distribution. A time-dependent flow moves a point by the ordinary differential equation</p> \[\frac{d x_t}{dt} = v_t(x_t).\] <p>The initial sample is $x_0 \sim p_0$.</p> <p>The distribution of $x_t$ changes over time. We can call this changing distribution $p_t$. The ideal goal is to choose a velocity field $v_t$ such that $p_t$ starts at $p_0$ and ends at $p_1$.</p> <p>The unavailable object is the direct marginal velocity target, not the conditional velocity target from sampled endpoints used below. At intermediate times, data do not give the exact marginal distribution $p_t$ or the global field $v_t$ that transports it. Conditional flow matching sidesteps that missing marginal target by conditioning on sampled endpoints, where the velocity along the chosen path is available.</p> <h2 id="path-and-velocity-target">Path and velocity target</h2> <p>Sample a source point $x_0 \sim p_0$, a data point $x_1 \sim p_1$, and a time $t \sim \mathcal{U}(0, 1)$. The simplest conditional path is a straight line:</p> \[x_t = (1 - t)x_0 + t x_1.\] <p>The velocity along this path is its derivative:</p> \[u_t = \frac{d x_t}{dt} = x_1 - x_0.\] <p>This turns the problem into supervised velocity prediction. The network sees the current position $x_t$ and time $t$, then predicts the velocity that should move the sample along the path.</p> <pre><code class="language-mermaid">flowchart LR
    accTitle: Flow Matching Pipeline
    accDescr: The diagram shows the flow matching training and sampling pipeline: sample endpoints, form a conditional path, regress the velocity, then integrate the learned field.

    source["Source x0 ~ p0"]
    data["Data x1 ~ p1"]
    time["Sample t"]
    path["Point on path xt"]
    target["Velocity target ut = x1 - x0"]
    model["Network v_theta(xt, t)"]
    loss["MSE velocity loss"]
    solver["ODE solver"]
    generated["Generated sample"]

    source --&gt; path
    data --&gt; path
    time --&gt; path
    path --&gt; model
    source --&gt; target
    data --&gt; target
    target --&gt; loss
    model --&gt; loss
    model --&gt; solver
    solver --&gt; generated
</code></pre> <p>At this point, the learning problem is just supervised velocity prediction. The later sampling step uses the learned average direction at each location and time, rather than the original endpoint pair.</p> <h2 id="training-objective">Training objective</h2> <p>For the straight-line path, the training loss is</p> \[\mathcal{L}(\theta)=\mathbb{E}_{t,x_0,x_1}[\ell_\theta],\] \[\ell_\theta=\left\|v_\theta(x_t,t)-(x_1-x_0)\right\|_2^2.\] <p>This objective is useful because training does not solve the sampling ODE. Each step only needs four operations: sample endpoints, sample time, interpolate, and regress the velocity.</p> <h2 id="minimal-implementation">Minimal implementation</h2> <p>The training core for the straight-line conditional path is only a few lines.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>


<span class="k">def</span> <span class="nf">expand_time</span><span class="p">(</span><span class="n">t</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">x</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">:</span>
    <span class="k">while</span> <span class="n">t</span><span class="p">.</span><span class="n">ndim</span> <span class="o">&lt;</span> <span class="n">x</span><span class="p">.</span><span class="n">ndim</span><span class="p">:</span>
        <span class="n">t</span> <span class="o">=</span> <span class="n">t</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">t</span>


<span class="k">def</span> <span class="nf">interpolate</span><span class="p">(</span><span class="n">x0</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">x1</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">t</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">:</span>
    <span class="n">t</span> <span class="o">=</span> <span class="nf">expand_time</span><span class="p">(</span><span class="n">t</span><span class="p">,</span> <span class="n">x0</span><span class="p">)</span>
    <span class="nf">return </span><span class="p">(</span><span class="mf">1.0</span> <span class="o">-</span> <span class="n">t</span><span class="p">)</span> <span class="o">*</span> <span class="n">x0</span> <span class="o">+</span> <span class="n">t</span> <span class="o">*</span> <span class="n">x1</span>


<span class="k">def</span> <span class="nf">flow_matching_loss</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">data</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">:</span>
    <span class="n">x1</span> <span class="o">=</span> <span class="n">data</span>
    <span class="n">x0</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x1</span><span class="p">)</span>
    <span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">rand</span><span class="p">(</span><span class="n">x1</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">device</span><span class="o">=</span><span class="n">x1</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>

    <span class="n">xt</span> <span class="o">=</span> <span class="nf">interpolate</span><span class="p">(</span><span class="n">x0</span><span class="p">,</span> <span class="n">x1</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>
    <span class="n">velocity_target</span> <span class="o">=</span> <span class="n">x1</span> <span class="o">-</span> <span class="n">x0</span>
    <span class="n">velocity_pred</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">xt</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>

    <span class="k">return</span> <span class="n">torch</span><span class="p">.</span><span class="nf">mean</span><span class="p">((</span><span class="n">velocity_pred</span> <span class="o">-</span> <span class="n">velocity_target</span><span class="p">)</span> <span class="o">**</span> <span class="mi">2</span><span class="p">)</span>
</code></pre></div></div> <p>This is the training target in code. <code class="language-plaintext highlighter-rouge">x0</code> is fresh Gaussian noise, <code class="language-plaintext highlighter-rouge">x1</code> is a batch of real data, <code class="language-plaintext highlighter-rouge">t</code> chooses a point between them, <code class="language-plaintext highlighter-rouge">xt</code> is that interpolated point, and <code class="language-plaintext highlighter-rouge">x1 - x0</code> is the velocity the model learns to predict.</p> <h2 id="code-result">Code result</h2> <p>A 2D check with a linear velocity model and a two-cluster target reduced the velocity-regression loss from 3.479 to 2.010 over 520 steps. The run is small, but it shows the training signal doing the right job: the model sees interpolated points and learns the velocity direction that moves them toward the sampled data endpoint.</p> <figure> <picture> <img src="/assets/img/blog/flow-matching-guide/flow-matching-loss.svg" class="img-fluid rounded z-depth-1" width="760" height="360" alt="Velocity-regression loss curve for a 2D flow matching check." data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>The path plot makes the sampling side concrete. Black dots are initial source samples, blue curves are Euler-integrated trajectories under the learned field, and red points are the target data cloud.</p> <figure> <picture> <img src="/assets/img/blog/flow-matching-guide/flow-matching-paths.svg" class="img-fluid rounded z-depth-1" width="760" height="460" alt="Paths moving source samples toward a two-cluster target distribution." data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>The limitation is also visible. Straight lines between independently paired noise and data points are easy to train on, but they are not always the best path for every domain.</p> <h2 id="sampling-procedure">Sampling procedure</h2> <p>There is one theoretical step to connect training and sampling. Training uses conditional endpoint pairs $(x_0, x_1)$, but sampling starts from fresh noise without choosing a target endpoint. Flow matching shows that the conditional regression objective has the right population optimum for the marginal velocity field under the chosen probability path. This conditional-to-marginal argument is the core idea behind flow matching and conditional flow matching <a href="https://arxiv.org/abs/2210.02747">Flow Matching for Generative Modeling</a>, <a href="https://arxiv.org/abs/2302.00482">Improving and Generalizing Flow-Based Generative Models with Minibatch Optimal Transport</a>.</p> <p>After training, discard the paired endpoint construction. Sampling starts from fresh noise and follows the learned vector field:</p> \[\frac{d x_t}{dt} = v_\theta(x_t,t).\] <p>Again, the initial sample is $x_0 \sim p_0$.</p> <p>A numerical ODE solver approximates</p> \[x_1 \approx x_0+\int_0^1 v_\theta(x_t,t)\,dt.\] <p>The number of solver steps controls the speed-quality tradeoff. More steps usually track the learned field more accurately; fewer steps are faster but can expose errors in the learned trajectory. This is why path design matters: rectified flow studies straight transports for fast sampling <a href="https://arxiv.org/abs/2209.03003">Flow Straight and Fast</a>, while stochastic interpolants connect flow and diffusion viewpoints through broader interpolating processes <a href="https://arxiv.org/abs/2303.08797">Stochastic Interpolants</a>. The official <a href="https://github.com/facebookresearch/flow_matching"><code class="language-plaintext highlighter-rouge">facebookresearch/flow_matching</code></a> library is the best place to check current implementation patterns.</p> <h2 id="next-part">Next part</h2> <p>Part 2 will implement the sampling loop and compare how ODE step count and path choice change the generated samples.</p> <h2 id="references-and-visual-resources">References and visual resources</h2> <ul> <li>Primary guide and paper: <a href="https://arxiv.org/abs/2412.06264">Flow Matching Guide and Code</a>.</li> <li>Core paper: <a href="https://arxiv.org/abs/2210.02747">Flow Matching for Generative Modeling</a>.</li> <li>Conditional flow matching and minibatch OT: <a href="https://arxiv.org/abs/2302.00482">Improving and Generalizing Flow-Based Generative Models with Minibatch Optimal Transport</a>.</li> <li>Related straight-path view: <a href="https://arxiv.org/abs/2209.03003">Flow Straight and Fast: Learning to Generate and Transfer Data with Rectified Flow</a>.</li> <li>Related interpolant view: <a href="https://arxiv.org/abs/2303.08797">Stochastic Interpolants: A Unifying Framework for Flows and Diffusions</a>.</li> <li>Official codebase: <a href="https://github.com/facebookresearch/flow_matching"><code class="language-plaintext highlighter-rouge">facebookresearch/flow_matching</code></a>.</li> <li>Visual reference: <a href="https://dl.heeere.com/conditional-flow-matching/blog/conditional-flow-matching/">A Visual Dive into Conditional Flow Matching</a> uses diagrams for the probability path and velocity field.</li> <li>Compact technical reference: <a href="https://www.weideng.org/posts/flow_matching/">Flow Matching: A Minimal Guide</a> lays out the ODE, continuity equation, and flow matching loss.</li> <li>Implementation-oriented walkthrough: <a href="https://daddaops.com/blog/flow-matching-from-scratch/">Flow Matching from Scratch</a> shows the straight-line path and training loop in a beginner-friendly way.</li> </ul>]]></content><author><name>Quan Tran Hong</name></author><category term="tutorial"/><category term="flow-matching"/><category term="tutorial"/><category term="reading-notes"/><category term="generative-modeling"/><summary type="html"><![CDATA[Train the basic velocity target.]]></summary></entry><entry><title type="html">Flow Matching: ODE Sampling</title><link href="https://quanth238.github.io/blog/2026/flow-matching-guide-part-2/" rel="alternate" type="text/html" title="Flow Matching: ODE Sampling"/><published>2026-05-19T00:00:00+00:00</published><updated>2026-05-19T00:00:00+00:00</updated><id>https://quanth238.github.io/blog/2026/flow-matching-guide-part-2</id><content type="html" xml:base="https://quanth238.github.io/blog/2026/flow-matching-guide-part-2/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>Sampling is the moment where the trained velocity field becomes a generator. Start from fresh source noise, evaluate $v_\theta(x,t)$ at the current state, and take numerical ODE steps until $t=1$. The Flow Matching Guide and Code paper presents this as integrating the learned vector field after training <a href="https://arxiv.org/abs/2412.06264">Flow Matching Guide and Code</a>, and the original flow matching paper frames the same object as a continuous normalizing flow velocity field <a href="https://arxiv.org/abs/2210.02747">Flow Matching for Generative Modeling</a>.</p> <p>The practical question is small but important: if the field is fixed, what does the solver actually do, and what changes when the step count changes?</p> <h2 id="problem-setup">Problem setup</h2> <p>Assume the model already learned a time-dependent velocity field $v_\theta(x,t)$. Generation starts with a batch of source samples:</p> \[x_0 \sim p_0.\] <p>The sampler follows the ODE</p> \[\frac{d x_t}{dt}=v_\theta(x_t,t).\] <p>The exact endpoint would be</p> \[x_1=x_0+\int_0^1 v_\theta(x_t,t)\,dt.\] <p>That integral is not evaluated in closed form for a neural field. A solver approximates it by querying the model at a finite number of times.</p> <h2 id="path-and-velocity-target">Path and velocity target</h2> <p>The straight conditional path from the first part gives the training target</p> \[u_t=x_1-x_0.\] <p>Sampling no longer has access to a paired data endpoint. The sampler only sees the current state $x_t$, the current time $t$, and the learned field $v_\theta(x_t,t)$. This distinction is useful: training constructs supervised velocity targets from endpoint pairs; sampling integrates the marginal field learned from those targets.</p> <h2 id="training-objective">Training objective</h2> <p>There is no new loss in the sampling loop. The same velocity regression objective trains the field:</p> \[\mathcal{L}(\theta)=\mathbb{E}\left[\|v_\theta(x_t,t)-u_t\|_2^2\right].\] <p>Once training is done, solver behavior is a numerical question. A coarse solver may follow the field with visible discretization error. A finer solver spends more evaluations to track the same field more closely. For this toy example, I use a 128-step Euler run as a reference and compare coarser Euler runs against it.</p> <h2 id="minimal-implementation">Minimal implementation</h2> <p>The simplest sampler is explicit Euler. Divide $[0,1]$ into <code class="language-plaintext highlighter-rouge">steps</code> equal intervals, query the velocity field, and update the state:</p> \[x_{t+\Delta t}=x_t+\Delta t\,v_\theta(x_t,t).\] <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>


<span class="nd">@torch.no_grad</span><span class="p">()</span>
<span class="k">def</span> <span class="nf">euler_sample</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">x0</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">steps</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">:</span>
    <span class="n">x</span> <span class="o">=</span> <span class="n">x0</span>
    <span class="n">dt</span> <span class="o">=</span> <span class="mf">1.0</span> <span class="o">/</span> <span class="n">steps</span>
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">steps</span><span class="p">):</span>
        <span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">full</span><span class="p">((</span><span class="n">x</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">],),</span> <span class="n">i</span> <span class="o">/</span> <span class="n">steps</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">x</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
        <span class="n">velocity</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>
        <span class="n">x</span> <span class="o">=</span> <span class="n">x</span> <span class="o">+</span> <span class="n">dt</span> <span class="o">*</span> <span class="n">velocity</span>
    <span class="k">return</span> <span class="n">x</span>
</code></pre></div></div> <p>The hand-written loop is the sampler used below. For orientation, <a href="https://github.com/facebookresearch/flow_matching"><code class="language-plaintext highlighter-rouge">flow_matching==1.0.10</code></a> exposes the same continuous-sampling idea through <code class="language-plaintext highlighter-rouge">flow_matching.solver.ODESolver</code>; Part 6 maps the toy pieces to the package API in detail. A minimal Euler call has the same ingredients: an initial batch, a velocity model, a method, and a time grid.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>
<span class="kn">from</span> <span class="n">flow_matching.solver</span> <span class="kn">import</span> <span class="n">ODESolver</span>


<span class="n">solver</span> <span class="o">=</span> <span class="nc">ODESolver</span><span class="p">(</span><span class="n">velocity_model</span><span class="o">=</span><span class="n">velocity_model</span><span class="p">)</span>
<span class="n">samples</span> <span class="o">=</span> <span class="n">solver</span><span class="p">.</span><span class="nf">sample</span><span class="p">(</span>
    <span class="n">x_init</span><span class="o">=</span><span class="n">x0</span><span class="p">,</span>
    <span class="n">step_size</span><span class="o">=</span><span class="mf">1.0</span> <span class="o">/</span> <span class="mi">32</span><span class="p">,</span>
    <span class="n">method</span><span class="o">=</span><span class="sh">"</span><span class="s">euler</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">time_grid</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="nf">tensor</span><span class="p">([</span><span class="mf">0.0</span><span class="p">,</span> <span class="mf">1.0</span><span class="p">],</span> <span class="n">device</span><span class="o">=</span><span class="n">x0</span><span class="p">.</span><span class="n">device</span><span class="p">),</span>
<span class="p">)</span>
</code></pre></div></div> <p>For the step-count diagnostic here, the hand-written Euler loop is enough because it exposes each model evaluation and update directly.</p> <h2 id="code-result">Code result</h2> <p>The figure uses one trained 2D toy field and the same initial noise samples for every panel. Only the number of Euler steps changes. Black dots are starts, blue curves are solver trajectories, teal dots are coarse final samples, purple rings are 128-step reference endpoints, orange segments are endpoint drift to that reference, and the vermillion cloud is the target distribution.</p> <figure> <picture> <img src="/assets/img/blog/flow-matching-guide/flow-matching-solver-steps.svg" class="img-fluid rounded z-depth-1" width="1180" height="760" alt="Euler solver step sweep showing 4, 8, 16, and 32 step trajectories with orange endpoint residuals to purple 128-step reference endpoints." data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>The endpoint drift compares each coarse Euler endpoint to a 128-step Euler reference on the same initial samples. The blue paths are intentionally similar because the field and starts are fixed; the orange residuals and the drift column are the step-count diagnostic.</p> <table> <thead> <tr> <th style="text-align: right">Euler steps</th> <th style="text-align: right">Mean reference drift</th> <th style="text-align: right">Mean nearest-mode distance</th> </tr> </thead> <tbody> <tr> <td style="text-align: right">4</td> <td style="text-align: right">0.239</td> <td style="text-align: right">1.423</td> </tr> <tr> <td style="text-align: right">8</td> <td style="text-align: right">0.126</td> <td style="text-align: right">1.476</td> </tr> <tr> <td style="text-align: right">16</td> <td style="text-align: right">0.062</td> <td style="text-align: right">1.509</td> </tr> <tr> <td style="text-align: right">32</td> <td style="text-align: right">0.027</td> <td style="text-align: right">1.527</td> </tr> </tbody> </table> <p>The drift decreases as the step count increases because the coarse integrator is getting closer to the fine Euler reference for this fixed field. The nearest-mode distance does not monotonically improve here, which is a useful warning: a solver metric and a data-quality metric are not the same object.</p> <h2 id="sampling-procedure">Sampling procedure</h2> <p>A practical sampling loop has four moving parts.</p> <p>First, choose the initial distribution. In the toy code this is standard Gaussian noise in two dimensions. In larger models it is still usually an easy source distribution.</p> <p>Second, choose the time grid. A uniform grid is the easiest first choice:</p> \[t_i=\frac{i}{N}.\] <p>Third, choose the numerical method. Euler is readable and cheap. Midpoint or higher-order solvers add extra velocity evaluations per step, which can reduce numerical error for the same grid length. Adaptive solvers such as <code class="language-plaintext highlighter-rouge">dopri5</code> choose internal steps based on error tolerances, so their <code class="language-plaintext highlighter-rouge">step_size</code> handling differs from fixed-step Euler-style loops.</p> <p>Fourth, decide what diagnostic you trust. In a toy plot, reference drift checks numerical integration against a finer solver. It does not prove that the learned field matches the data distribution. Sample plots, target-distance summaries, and trajectory shape all answer different questions.</p> <h2 id="next-part">Next part</h2> <p>Part 3 derives why endpoint-conditioned targets recover the marginal velocity used by the sampler.</p> <h2 id="references-and-visual-resources">References and visual resources</h2> <ul> <li>Primary guide and codebase paper: <a href="https://arxiv.org/abs/2412.06264">Flow Matching Guide and Code</a>.</li> <li>Core paper: <a href="https://arxiv.org/abs/2210.02747">Flow Matching for Generative Modeling</a>.</li> <li>Official package: <a href="https://github.com/facebookresearch/flow_matching"><code class="language-plaintext highlighter-rouge">facebookresearch/flow_matching</code></a>.</li> <li>Visual reference: <a href="https://dl.heeere.com/conditional-flow-matching/blog/conditional-flow-matching/">A Visual Dive into Conditional Flow Matching</a> uses probability-path diagrams that are helpful context for reading solver trajectories.</li> <li>Implementation-oriented walkthrough: <a href="https://daddaops.com/blog/flow-matching-from-scratch/">Flow Matching from Scratch</a> gives a compact straight-line training loop before moving to sampling.</li> </ul>]]></content><author><name>Quan Tran Hong</name></author><category term="tutorial"/><category term="flow-matching"/><category term="tutorial"/><category term="reading-notes"/><category term="generative-modeling"/><summary type="html"><![CDATA[Compare coarse and fine ODE steps.]]></summary></entry><entry><title type="html">Flow Matching: Local Averages</title><link href="https://quanth238.github.io/blog/2026/flow-matching-guide-part-3/" rel="alternate" type="text/html" title="Flow Matching: Local Averages"/><published>2026-05-19T00:00:00+00:00</published><updated>2026-05-19T00:00:00+00:00</updated><id>https://quanth238.github.io/blog/2026/flow-matching-guide-part-3</id><content type="html" xml:base="https://quanth238.github.io/blog/2026/flow-matching-guide-part-3/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>The sampler in Part 2 never chooses a target endpoint. It starts from noise and follows $v_\theta(x,t)$. Training, however, used endpoint pairs. The Flow Matching Guide states the bridge directly: under the marginalization trick, conditional velocity fields can be averaged into a marginal velocity field that generates the marginal probability path <a href="https://arxiv.org/abs/2412.06264">Flow Matching Guide and Code</a>. The original paper gives the same construction for conditional probability paths and states that the Flow Matching and Conditional Flow Matching objectives have identical gradients <a href="https://arxiv.org/abs/2210.02747">Flow Matching for Generative Modeling</a>.</p> <p>The useful way to read the result is local. Around one position and one time, many endpoint pairs may pass nearby. Each pair proposes a conditional velocity. The marginal field is the average direction that the sampler should use at that location.</p> <pre><code class="language-mermaid">flowchart LR
    accTitle: Conditional To Marginal Flow Matching Argument
    accDescr: The diagram separates the unavailable marginal flow matching loss from the tractable conditional flow matching loss and shows that population averaging recovers the marginal velocity used by sampling.

    endpoints["Sample endpoint pair"]
    path["Build conditional point Xt"]
    cond["Conditional velocity Ut"]
    cfm["Conditional FM loss"]
    avg["Average over pairs near (x,t)"]
    marginal["Marginal velocity ut(x)"]
    sampler["ODE sampler uses vtheta(x,t)"]
    ideal["Ideal FM loss"]

    endpoints --&gt; path
    endpoints --&gt; cond
    path --&gt; cfm
    cond --&gt; cfm
    cond --&gt; avg
    avg --&gt; marginal
    marginal --&gt; ideal
    cfm --&gt; sampler
    ideal --&gt; sampler
</code></pre> <h2 id="problem-setup">Problem setup</h2> <p>Let $Z$ denote the conditioning information used to create a training target. In the straight endpoint-pair version, $Z=(X_0,X_1)$. At a sampled time $t$, the conditional point is</p> \[X_t=(1-t)X_0+tX_1.\] <p>The endpoint-conditioned velocity is</p> \[U_t=X_1-X_0.\] <p>During training, the model sees $(X_t,t)$ and regresses toward $U_t$. During sampling, $Z$ is absent. A generated point has only its current state and time.</p> <h2 id="path-and-velocity-target">Path and velocity target</h2> <p>The marginal field is the conditional average of the endpoint-conditioned velocity at the current location:</p> \[u_t(x)=\mathbb{E}[U_t\mid X_t=x].\] <p>For a more general conditioning variable $Z$, the guide writes the same idea as an average of conditional velocities:</p> \[u_t(x)=\mathbb{E}[u_t(X_t|Z)\mid X_t=x].\] <p>This is the field the sampler wants. It is not a single endpoint arrow. It is the average of all conditional arrows compatible with the same local state.</p> <p>There are several valid choices for what goes into $Z$. The next formula is target-conditioned, not paired-endpoint conditioned: it fixes the data endpoint $x_1$ and writes the velocity as a function of the current state $x$. If the path is conditioned only on $x_1$, the conditional velocity is</p> \[u_t(x|x_1)=\frac{x_1-x}{1-t}.\] <p>The paired-endpoint implementation uses the same averaging principle with $Z=(X_0,X_1)$ and the simple velocity $X_1-X_0$.</p> <h2 id="training-objective">Training objective</h2> <p>The ideal marginal Flow Matching loss would regress directly to $u_t(X_t)$:</p> \[\mathcal{L}_{\mathrm{FM}}(\theta)= \mathbb{E}\left[\|v_\theta(X_t,t)-u_t(X_t)\|_2^2\right].\] <p>That target is usually unavailable because it already requires the local average over endpoint pairs. Conditional Flow Matching uses the tractable target:</p> \[\mathcal{L}_{\mathrm{CFM}}(\theta)= \mathbb{E}\left[\|v_\theta(X_t,t)-U_t\|_2^2\right].\] <p>The population-level statement used here has a narrow scope: it compares the marginal and conditional objectives for the same conditional path, under the regularity assumptions that justify differentiating and marginalizing the loss. In that setting, the gradient equivalence is</p> \[\nabla_\theta \mathcal{L}_{\mathrm{FM}}(\theta)= \nabla_\theta \mathcal{L}_{\mathrm{CFM}}(\theta).\] <p>Within that population objective, the minimizer of the conditional objective is the marginal velocity. This is why endpoint-conditioned targets can train a field that later samples without a chosen endpoint.</p> <h2 id="minimal-implementation">Minimal implementation</h2> <p>The code target is still the same regression loop. The only extra diagnostic is to inspect the average of conditional arrows near a location.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>


<span class="k">def</span> <span class="nf">endpoint_batch</span><span class="p">(</span><span class="n">data</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">tuple</span><span class="p">[</span><span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">]:</span>
    <span class="n">x1</span> <span class="o">=</span> <span class="n">data</span>
    <span class="n">x0</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x1</span><span class="p">)</span>
    <span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">rand</span><span class="p">(</span><span class="n">x1</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">device</span><span class="o">=</span><span class="n">x1</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
    <span class="n">xt</span> <span class="o">=</span> <span class="p">(</span><span class="mf">1.0</span> <span class="o">-</span> <span class="n">t</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">])</span> <span class="o">*</span> <span class="n">x0</span> <span class="o">+</span> <span class="n">t</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">x1</span>
    <span class="n">target_velocity</span> <span class="o">=</span> <span class="n">x1</span> <span class="o">-</span> <span class="n">x0</span>
    <span class="k">return</span> <span class="n">xt</span><span class="p">,</span> <span class="n">t</span><span class="p">,</span> <span class="n">target_velocity</span>


<span class="k">def</span> <span class="nf">local_average_velocity</span><span class="p">(</span>
    <span class="n">xt</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span>
    <span class="n">target_velocity</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span>
    <span class="n">query</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span>
    <span class="n">radius</span><span class="p">:</span> <span class="nb">float</span><span class="p">,</span>
<span class="p">)</span> <span class="o">-&gt;</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">:</span>
    <span class="n">keep</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="nf">norm</span><span class="p">(</span><span class="n">xt</span> <span class="o">-</span> <span class="n">query</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="p">:],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span> <span class="o">&lt;=</span> <span class="n">radius</span>
    <span class="k">return</span> <span class="n">target_velocity</span><span class="p">[</span><span class="n">keep</span><span class="p">].</span><span class="nf">mean</span><span class="p">(</span><span class="n">dim</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
</code></pre></div></div> <p>The first function is the training target. The second function is a diagnostic version of the marginalization idea: keep endpoint pairs whose interpolated point lies near the query and average their velocities.</p> <h2 id="code-result">Code result</h2> <p>The local plot fixes $t=0.55$ and marks one query location. The left panel shows which interpolated points form the neighborhood. The right panel zooms into that neighborhood: gray arrows are endpoint-conditioned velocity directions from sampled pairs, and the teal arrow is their empirical local average.</p> <figure> <picture> <img src="/assets/img/blog/flow-matching-guide/flow-matching-conditional-marginal-arrows.svg" class="img-fluid rounded z-depth-1" width="1120" height="680" alt="Two-panel conditional-to-marginal diagnostic showing a query neighborhood and conditional endpoint velocity arrows averaged into an empirical local field estimate." data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>The run sampled 5,000 endpoint pairs and kept the 26 nearest interpolated points around the query. The local average velocity was approximately $(-0.126, 0.131)$. The exact number is not the point; the useful object is the distinction between many conditional arrows and one finite-sample estimate of the marginal direction.</p> <h2 id="sampling-procedure">Sampling procedure</h2> <p>Sampling uses the learned approximation to the marginal field:</p> \[\frac{dX_t}{dt}=v_\theta(X_t,t).\] <p>No target endpoint is sampled during generation. The endpoint construction lives in the training objective, where it supplies cheap supervised velocity targets. At population scale, the conditional objective pushes the model toward the same marginal velocity field that the ODE sampler needs.</p> <p>This also explains why Part 2 could integrate a field without carrying endpoint labels. The solver does not know which data point a generated sample will approach. It only evaluates a learned average direction at the current location and time.</p> <h2 id="next-part">Next part</h2> <p>Part 4 changes the endpoint coupling and measures how pairing alters path geometry.</p> <h2 id="references-and-visual-resources">References and visual resources</h2> <ul> <li>Primary guide and codebase paper: <a href="https://arxiv.org/abs/2412.06264">Flow Matching Guide and Code</a>.</li> <li>Core paper: <a href="https://arxiv.org/abs/2210.02747">Flow Matching for Generative Modeling</a>.</li> <li>Conditional flow matching and minibatch OT: <a href="https://arxiv.org/abs/2302.00482">Improving and Generalizing Flow-Based Generative Models with Minibatch Optimal Transport</a>.</li> <li>Visual reference: <a href="https://dl.heeere.com/conditional-flow-matching/blog/conditional-flow-matching/">A Visual Dive into Conditional Flow Matching</a> uses path and velocity diagrams that help separate conditional and marginal objects.</li> <li>Compact technical reference: <a href="https://www.weideng.org/posts/flow_matching/">Flow Matching: A Minimal Guide</a> gives a concise conditional-expectation view of the training target.</li> </ul>]]></content><author><name>Quan Tran Hong</name></author><category term="tutorial"/><category term="flow-matching"/><category term="tutorial"/><category term="reading-notes"/><category term="generative-modeling"/><summary type="html"><![CDATA[Average arrows into a field.]]></summary></entry><entry><title type="html">Flow Matching: Endpoint Pairing</title><link href="https://quanth238.github.io/blog/2026/flow-matching-guide-part-4/" rel="alternate" type="text/html" title="Flow Matching: Endpoint Pairing"/><published>2026-05-19T00:00:00+00:00</published><updated>2026-05-19T00:00:00+00:00</updated><id>https://quanth238.github.io/blog/2026/flow-matching-guide-part-4</id><content type="html" xml:base="https://quanth238.github.io/blog/2026/flow-matching-guide-part-4/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>Endpoint pairing is a modeling choice. The path formula can stay simple, while the coupling that chooses which source sample pairs with which data sample changes the training targets. Conditional Flow Matching makes this choice explicit: different couplings define different conditional paths, and the marginal field is learned by averaging those conditional velocities <a href="https://arxiv.org/abs/2412.06264">Flow Matching Guide and Code</a>.</p> <p>OT-CFM studies this choice through optimal-transport-inspired couplings. The paper reports simpler flows and faster inference in its experiments, and it states the true-OT case as an approximation to dynamic OT <a href="https://arxiv.org/abs/2302.00482">Improving and generalizing flow-based generative models with minibatch optimal transport</a>. In code, the common lightweight version is a minibatch assignment. That assignment is useful, but it is only an optimization inside the current batch.</p> <h2 id="problem-setup">Problem setup</h2> <p>Let $\pi(x_0,x_1)$ be the coupling between source and data endpoints. The independent coupling samples the two endpoints separately:</p> \[X_0\sim p_0,\] \[X_1\sim p_1.\] <p>A transport-aware coupling tries to pair nearby or compatible endpoints more deliberately. Inside one minibatch, that often means solving an assignment problem over the batch cost matrix:</p> \[C_{ij}=\|x_0^{(i)}-x_1^{(j)}\|_2^2.\] <p>The result is a permutation of target samples for this batch. It is not the full-data OT plan; it is a finite-batch pairing rule.</p> <h2 id="path-and-velocity-target">Path and velocity target</h2> <p>Once a pair is chosen, the straight conditional path is unchanged:</p> \[X_t=(1-t)X_0+tX_1.\] <p>The velocity target is also unchanged:</p> \[U_t=X_1-X_0.\] <p>The coupling changes which $X_1$ appears next to each $X_0$. That changes the segment lengths, the line crossings, and the distribution of velocity targets seen by the model.</p> <h2 id="training-objective">Training objective</h2> <p>With a coupling $\pi$, the conditional objective is</p> \[\mathcal{L}_{\mathrm{CFM}}(\theta)= \mathbb{E}_{t,(X_0,X_1)\sim\pi} \left[\|v_\theta(X_t,t)-(X_1-X_0)\|_2^2\right].\] <p>The loss shape is the same for independent pairing and minibatch assignment. The data distribution over endpoint pairs is different.</p> <p>Rectified Flow gives a second reason to care about path geometry. It learns ODE models that follow straight paths between the two observed distributions as much as possible, and reports that increasingly straight paths can be simulated accurately with coarse time discretization <a href="https://arxiv.org/abs/2209.03003">Flow Straight and Fast</a>. That does not make minibatch OT and Rectified Flow the same algorithm. It gives a shared geometric diagnostic: straighter, shorter, less tangled paths are the object to inspect before making any claim about solver behavior.</p> <h2 id="minimal-implementation">Minimal implementation</h2> <p>A batch-local assignment needs a cost matrix and an assignment solver. The important restriction is the scope: the optimizer sees only the current minibatch.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>
<span class="kn">from</span> <span class="n">scipy.optimize</span> <span class="kn">import</span> <span class="n">linear_sum_assignment</span>


<span class="k">def</span> <span class="nf">minibatch_ot_pairs</span><span class="p">(</span><span class="n">x0</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">x1</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">tuple</span><span class="p">[</span><span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">]:</span>
    <span class="n">cost</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cdist</span><span class="p">(</span><span class="n">x0</span><span class="p">,</span> <span class="n">x1</span><span class="p">).</span><span class="nf">pow</span><span class="p">(</span><span class="mi">2</span><span class="p">)</span>
    <span class="n">row</span><span class="p">,</span> <span class="n">col</span> <span class="o">=</span> <span class="nf">linear_sum_assignment</span><span class="p">(</span><span class="n">cost</span><span class="p">.</span><span class="nf">detach</span><span class="p">().</span><span class="nf">cpu</span><span class="p">().</span><span class="nf">numpy</span><span class="p">())</span>
    <span class="n">row</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">as_tensor</span><span class="p">(</span><span class="n">row</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">x0</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
    <span class="n">col</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">as_tensor</span><span class="p">(</span><span class="n">col</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">x1</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">x0</span><span class="p">[</span><span class="n">row</span><span class="p">],</span> <span class="n">x1</span><span class="p">[</span><span class="n">col</span><span class="p">]</span>


<span class="k">def</span> <span class="nf">straight_targets</span><span class="p">(</span><span class="n">x0</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">x1</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">t</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">):</span>
    <span class="n">xt</span> <span class="o">=</span> <span class="p">(</span><span class="mf">1.0</span> <span class="o">-</span> <span class="n">t</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">])</span> <span class="o">*</span> <span class="n">x0</span> <span class="o">+</span> <span class="n">t</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">x1</span>
    <span class="n">velocity</span> <span class="o">=</span> <span class="n">x1</span> <span class="o">-</span> <span class="n">x0</span>
    <span class="k">return</span> <span class="n">xt</span><span class="p">,</span> <span class="n">velocity</span>
</code></pre></div></div> <p>This changes the pairing, not the regression formula. The same model still learns $v_\theta(x,t)$ from interpolated points and velocity targets.</p> <h2 id="code-result">Code result</h2> <p>The diagnostic below uses the same source and target minibatch for both panels. The left panel shuffles endpoints independently. The right panel uses the minimum squared-distance assignment inside the displayed minibatch.</p> <figure> <picture> <img src="/assets/img/blog/flow-matching-guide/flow-matching-coupling-diagnostics.svg" class="img-fluid rounded z-depth-1" width="1100" height="620" alt="Endpoint coupling comparison showing independent random pairing and minibatch optimal transport assignment on the same toy batch." data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <table> <thead> <tr> <th>Pairing rule</th> <th style="text-align: right">Mean squared endpoint cost</th> <th style="text-align: right">Segment crossings</th> </tr> </thead> <tbody> <tr> <td>Independent random pairing</td> <td style="text-align: right">7.430</td> <td style="text-align: right">16</td> </tr> <tr> <td>Minibatch OT assignment</td> <td style="text-align: right">3.917</td> <td style="text-align: right">3</td> </tr> </tbody> </table> <p>The batch assignment reduces this toy transport cost and removes many line crossings. That is a path-geometry result for the displayed batch. It does not say the learned model has solved the global OT problem.</p> <h2 id="sampling-procedure">Sampling procedure</h2> <p>The sampler still integrates the learned marginal field. Coupling affects sampling indirectly by changing the conditional velocities used during training.</p> <p>A tangled independent pairing can ask the model to explain many long crossing segments. A batch assignment often gives shorter endpoint targets in the displayed minibatch. That geometry is a hypothesis about easier integration, not a sampling-cost measurement in this toy result. Rectified-flow work studies the stronger coarse-discretization question separately by training toward straighter transports.</p> <p>The practical diagnostic in this part is simple: inspect endpoint cost and line crossings for the same displayed batch. These numbers say whether the pairing changed local path geometry. Solver-step sensitivity would need a separate experiment, so it is a hypothesis for later inspection rather than a conclusion from this figure.</p> <h2 id="next-part">Next part</h2> <p>Part 5 changes the probability path itself and connects flow matching to diffusion-style paths.</p> <h2 id="references-and-visual-resources">References and visual resources</h2> <ul> <li>Conditional flow matching and minibatch OT: <a href="https://arxiv.org/abs/2302.00482">Improving and generalizing flow-based generative models with minibatch optimal transport</a>.</li> <li>Rectified paths and coarse solvers: <a href="https://arxiv.org/abs/2209.03003">Flow Straight and Fast: Learning to Generate and Transfer Data with Rectified Flow</a>.</li> <li>Primary guide and codebase paper: <a href="https://arxiv.org/abs/2412.06264">Flow Matching Guide and Code</a>.</li> <li>Visual reference: <a href="https://dl.heeere.com/conditional-flow-matching/blog/conditional-flow-matching/">A Visual Dive into Conditional Flow Matching</a> uses pairing and path pictures that are useful for thinking about endpoint geometry.</li> </ul>]]></content><author><name>Quan Tran Hong</name></author><category term="tutorial"/><category term="flow-matching"/><category term="tutorial"/><category term="reading-notes"/><category term="generative-modeling"/><summary type="html"><![CDATA[Compare random and OT pairing.]]></summary></entry><entry><title type="html">Flow Matching: Path Design</title><link href="https://quanth238.github.io/blog/2026/flow-matching-guide-part-5/" rel="alternate" type="text/html" title="Flow Matching: Path Design"/><published>2026-05-19T00:00:00+00:00</published><updated>2026-05-19T00:00:00+00:00</updated><id>https://quanth238.github.io/blog/2026/flow-matching-guide-part-5</id><content type="html" xml:base="https://quanth238.github.io/blog/2026/flow-matching-guide-part-5/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>The endpoint coupling chooses which samples are paired. The probability path chooses how each pair moves through time. Flow Matching Guide and Code frames this as a scheduler choice inside affine conditional flows: choose $\alpha_t$ and $\sigma_t$, then form a path between source and data <a href="https://arxiv.org/abs/2412.06264">Flow Matching Guide and Code</a>.</p> <p>This is also where the diffusion connection becomes concrete. Gaussian paths with schedules $\alpha_t$ and $\sigma_t$ include flow-matching paths and diffusion-style probability paths. With a Gaussian source, independent coupling, and score parameterization, the guide connects diffusion probability-flow ODE sampling to flow matching with the corresponding Gaussian path. Stochastic interpolants give an even broader view that includes deterministic probability-flow equations and stochastic differential equations <a href="https://arxiv.org/abs/2303.08797">Stochastic Interpolants</a>.</p> <h2 id="problem-setup">Problem setup</h2> <p>Keep the same endpoint pair $(X_0,X_1)$. Instead of always using the straight interpolation, define an affine path:</p> \[X_t=\alpha_t X_1+\sigma_t X_0.\] <p>The scheduler satisfies the endpoint conditions</p> \[\alpha_0=0,\] \[\alpha_1=1,\] \[\sigma_0=1,\] \[\sigma_1=0.\] <p>The guide lists several schedulers. I will compare two:</p> <table> <thead> <tr> <th>Schedule</th> <th>$\alpha_t$</th> <th>$\sigma_t$</th> </tr> </thead> <tbody> <tr> <td>Conditional OT</td> <td>$t$</td> <td>$1-t$</td> </tr> <tr> <td>Linear VP</td> <td>$t$</td> <td>$\sqrt{1-t^2}$</td> </tr> </tbody> </table> <p>The second schedule keeps more source noise at middle times. That changes both what the model sees and the scale of the target velocity.</p> <h2 id="path-and-velocity-target">Path and velocity target</h2> <p>Different schedulers imply different conditional velocity targets. Differentiate the affine path:</p> \[U_t=\dot{\alpha}_t X_1+\dot{\sigma}_t X_0.\] <p>For Conditional OT,</p> \[\alpha_t=t,\] \[\sigma_t=1-t.\] <p>The target is</p> \[U_t=X_1-X_0.\] <p>For Linear VP,</p> \[\alpha_t=t,\] \[\sigma_t=\sqrt{1-t^2}.\] <p>The derivative gives</p> \[U_t=X_1-\frac{t}{\sqrt{1-t^2}}X_0.\] <p>The late-time velocity can become large because $\sigma_t=\sqrt{1-t^2}$ goes to zero as $t$ approaches 1, so the factor $t/\sigma_t$ is singular at the endpoint. That is a schedule effect, not a model architecture effect. In the toy comparison below, the reported times stay below 1, and the code uses a small epsilon guard when evaluating the schedule near the endpoint.</p> <h2 id="training-objective">Training objective</h2> <p>The loss still regresses the model toward the path velocity:</p> \[\mathcal{L}(\theta)= \mathbb{E}\left[\|v_\theta(X_t,t)-U_t\|_2^2\right].\] <p>Changing the path changes the distribution of $X_t$ and the target $U_t$. The same network and optimizer can face a different regression problem.</p> <p>Gaussian paths also explain the diffusion connection. The guide writes Gaussian conditional paths as</p> \[p_{t|1}(x|x_1)= \mathcal{N}(x\mid \alpha_t x_1,\sigma_t^2 I).\] <p>Diffusion forward processes with affine drift coefficients can be expressed as Gaussian probability paths after time reparameterization. With the right score parameterization, the probability-flow ODE is another way to sample the same marginal path.</p> <h2 id="minimal-implementation">Minimal implementation</h2> <p>The path family fits into one small scheduler object.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>


<span class="k">def</span> <span class="nf">cond_ot_schedule</span><span class="p">(</span><span class="n">t</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">tuple</span><span class="p">[</span><span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">]:</span>
    <span class="k">return</span> <span class="n">t</span><span class="p">,</span> <span class="mf">1.0</span> <span class="o">-</span> <span class="n">t</span>


<span class="k">def</span> <span class="nf">linear_vp_schedule</span><span class="p">(</span><span class="n">t</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">tuple</span><span class="p">[</span><span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">]:</span>
    <span class="k">return</span> <span class="n">t</span><span class="p">,</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">clamp</span><span class="p">(</span><span class="mf">1.0</span> <span class="o">-</span> <span class="n">t</span><span class="o">**</span><span class="mi">2</span><span class="p">,</span> <span class="nb">min</span><span class="o">=</span><span class="mf">1e-6</span><span class="p">))</span>


<span class="k">def</span> <span class="nf">affine_path</span><span class="p">(</span><span class="n">x0</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">x1</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">t</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">schedule</span><span class="p">):</span>
    <span class="n">alpha</span><span class="p">,</span> <span class="n">sigma</span> <span class="o">=</span> <span class="nf">schedule</span><span class="p">(</span><span class="n">t</span><span class="p">)</span>
    <span class="n">xt</span> <span class="o">=</span> <span class="n">alpha</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">x1</span> <span class="o">+</span> <span class="n">sigma</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">x0</span>
    <span class="k">return</span> <span class="n">xt</span>
</code></pre></div></div> <p>For training, the schedule also needs $\dot{\alpha}_t$ and $\dot{\sigma}_t$ so the velocity target matches the sampled path.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">linear_vp_velocity</span><span class="p">(</span><span class="n">x0</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">x1</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">t</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">):</span>
    <span class="n">sigma</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">clamp</span><span class="p">(</span><span class="mf">1.0</span> <span class="o">-</span> <span class="n">t</span><span class="o">**</span><span class="mi">2</span><span class="p">,</span> <span class="nb">min</span><span class="o">=</span><span class="mf">1e-6</span><span class="p">))</span>
    <span class="k">return</span> <span class="n">x1</span> <span class="o">-</span> <span class="p">(</span><span class="n">t</span> <span class="o">/</span> <span class="n">sigma</span><span class="p">)[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">x0</span>
</code></pre></div></div> <p>The clamp is the epsilon guard for the Linear VP endpoint. It keeps the toy computation finite if a sampled or plotted time gets too close to $t=1$; the comparison table evaluates times below the endpoint.</p> <h2 id="code-result">Code result</h2> <p>The figure uses the same endpoint pairs and the same x-y axes for both rows. The top row uses Conditional OT. The bottom row uses the guide-listed Linear VP schedule.</p> <figure> <picture> <img src="/assets/img/blog/flow-matching-guide/flow-matching-path-family-snapshots.svg" class="img-fluid rounded z-depth-1" width="1180" height="760" alt="Probability path snapshots comparing Conditional OT and Linear VP schedules on the same toy endpoint pairs and shared x-y axes." data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>The middle-time snapshots differ because the schedules mix source and data with different weights. Conditional OT moves mass linearly toward the target. Linear VP keeps a wider source-noise component for longer, then pays for that with larger late-time velocity magnitudes.</p> <table> <thead> <tr> <th>Schedule</th> <th style="text-align: right">$t=0.00$</th> <th style="text-align: right">$t=0.33$</th> <th style="text-align: right">$t=0.66$</th> <th style="text-align: right">$t=0.90$</th> </tr> </thead> <tbody> <tr> <td>Conditional OT mean velocity</td> <td style="text-align: right">2.50</td> <td style="text-align: right">2.50</td> <td style="text-align: right">2.50</td> <td style="text-align: right">2.50</td> </tr> <tr> <td>Linear VP mean velocity</td> <td style="text-align: right">2.29</td> <td style="text-align: right">2.31</td> <td style="text-align: right">2.45</td> <td style="text-align: right">3.35</td> </tr> </tbody> </table> <p>The numbers are conditional target magnitudes for the toy draw. They are useful because they show what the loss is asking the model to fit at each time.</p> <h2 id="sampling-procedure">Sampling procedure</h2> <p>The sampler must use a field trained for the chosen path. If the model was trained with Conditional OT targets, its velocity scale and time behavior match that path. If it was trained with a Gaussian or diffusion-like schedule, the solver queries a field with a different time profile.</p> <p>This is why path choice is not just a notation change. It changes intermediate distributions, loss weighting over time, target magnitude, and sometimes the best solver schedule. The diffusion link is the same idea in a broader form: choose a Gaussian probability path, train a score or velocity parameterization for that path, then sample with an ODE or SDE that follows the same marginals.</p> <h2 id="next-part">Next part</h2> <p>Part 6 maps the toy pieces to the official <code class="language-plaintext highlighter-rouge">flow_matching</code> package.</p> <h2 id="references-and-visual-resources">References and visual resources</h2> <ul> <li>Primary guide and codebase paper: <a href="https://arxiv.org/abs/2412.06264">Flow Matching Guide and Code</a>.</li> <li>Core paper: <a href="https://arxiv.org/abs/2210.02747">Flow Matching for Generative Modeling</a>.</li> <li>Related interpolant view: <a href="https://arxiv.org/abs/2303.08797">Stochastic Interpolants: A Unifying Framework for Flows and Diffusions</a>.</li> <li>Visual reference: <a href="https://dl.heeere.com/conditional-flow-matching/blog/conditional-flow-matching/">A Visual Dive into Conditional Flow Matching</a> uses probability-path pictures that are helpful for reading scheduler changes.</li> </ul>]]></content><author><name>Quan Tran Hong</name></author><category term="tutorial"/><category term="flow-matching"/><category term="tutorial"/><category term="reading-notes"/><category term="generative-modeling"/><summary type="html"><![CDATA[Compare two path schedules.]]></summary></entry><entry><title type="html">Flow Matching: Official API</title><link href="https://quanth238.github.io/blog/2026/flow-matching-guide-part-6/" rel="alternate" type="text/html" title="Flow Matching: Official API"/><published>2026-05-19T00:00:00+00:00</published><updated>2026-05-19T00:00:00+00:00</updated><id>https://quanth238.github.io/blog/2026/flow-matching-guide-part-6</id><content type="html" xml:base="https://quanth238.github.io/blog/2026/flow-matching-guide-part-6/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>The toy code has four moving parts: a probability path, a velocity target, a velocity model, and an ODE sampler. The official <a href="https://github.com/facebookresearch/flow_matching"><code class="language-plaintext highlighter-rouge">flow_matching</code></a> package keeps the same separation. The repository describes a PyTorch library for continuous and discrete flow matching implementations, with path, scheduler, solver, loss, and example modules tied to the Flow Matching Guide and Code paper <a href="https://arxiv.org/abs/2412.06264">Flow Matching Guide and Code</a>.</p> <p>In <code class="language-plaintext highlighter-rouge">flow_matching==1.0.10</code>, the package exports <code class="language-plaintext highlighter-rouge">CondOTProbPath</code>, <code class="language-plaintext highlighter-rouge">AffineProbPath</code>, scheduler classes such as <code class="language-plaintext highlighter-rouge">CondOTScheduler</code> and <code class="language-plaintext highlighter-rouge">LinearVPScheduler</code>, and <code class="language-plaintext highlighter-rouge">ODESolver</code> for continuous sampling.</p> <pre><code class="language-mermaid">flowchart LR
    accTitle: Toy Flow Matching To Official Package Components
    accDescr: The diagram maps the tutorial's toy path, scheduler, velocity target, model, and solver concepts to components in flow_matching version 1.0.10.

    toy_path["Toy path xt"]
    toy_velocity["Toy target Ut"]
    toy_model["Velocity model vtheta"]
    toy_loss["MSE velocity loss"]
    toy_solver["Euler or midpoint loop"]

    pkg_path["CondOTProbPath or AffineProbPath"]
    pkg_scheduler["CondOTScheduler or LinearVPScheduler"]
    pkg_sample["PathSample: x_t and dx_t"]
    pkg_model["Callable or ModelWrapper"]
    pkg_solver["ODESolver.sample"]

    toy_path --&gt; pkg_path
    pkg_scheduler --&gt; pkg_path
    pkg_path --&gt; pkg_sample
    toy_velocity --&gt; pkg_sample
    toy_model --&gt; pkg_model
    toy_loss --&gt; pkg_sample
    toy_solver --&gt; pkg_solver
    pkg_model --&gt; pkg_solver
</code></pre> <h2 id="problem-setup">Problem setup</h2> <p>The package does not remove the conceptual work. It gives names and tested APIs for the same objects:</p> <table> <thead> <tr> <th>Tutorial object</th> <th>Package object in <code class="language-plaintext highlighter-rouge">flow_matching==1.0.10</code></th> </tr> </thead> <tbody> <tr> <td>Conditional OT path</td> <td><code class="language-plaintext highlighter-rouge">CondOTProbPath</code></td> </tr> <tr> <td>General affine path</td> <td><code class="language-plaintext highlighter-rouge">AffineProbPath</code></td> </tr> <tr> <td>Conditional OT scheduler</td> <td><code class="language-plaintext highlighter-rouge">CondOTScheduler</code></td> </tr> <tr> <td>Linear VP scheduler</td> <td><code class="language-plaintext highlighter-rouge">LinearVPScheduler</code></td> </tr> <tr> <td>Continuous ODE sampler</td> <td><code class="language-plaintext highlighter-rouge">ODESolver</code></td> </tr> <tr> <td>Model wrapper option</td> <td><code class="language-plaintext highlighter-rouge">ModelWrapper</code></td> </tr> </tbody> </table> <p>For the continuous path used in this series, the path object returns a <code class="language-plaintext highlighter-rouge">PathSample</code> with <code class="language-plaintext highlighter-rouge">x_t</code> and <code class="language-plaintext highlighter-rouge">dx_t</code>. The training loop can then use a standard PyTorch regression loss between the model output and <code class="language-plaintext highlighter-rouge">dx_t</code>.</p> <h2 id="path-and-velocity-target">Path and velocity target</h2> <p>The package path object owns the schedule and target construction. For Conditional OT:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>
<span class="kn">from</span> <span class="n">flow_matching.path</span> <span class="kn">import</span> <span class="n">CondOTProbPath</span>


<span class="n">path</span> <span class="o">=</span> <span class="nc">CondOTProbPath</span><span class="p">()</span>
<span class="n">x0</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">batch_size</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>
<span class="n">x1</span> <span class="o">=</span> <span class="n">data_batch</span>
<span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">rand</span><span class="p">(</span><span class="n">batch_size</span><span class="p">)</span>

<span class="n">sample</span> <span class="o">=</span> <span class="n">path</span><span class="p">.</span><span class="nf">sample</span><span class="p">(</span><span class="n">x0</span><span class="p">,</span> <span class="n">x1</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>
<span class="n">xt</span> <span class="o">=</span> <span class="n">sample</span><span class="p">.</span><span class="n">x_t</span>
<span class="n">velocity_target</span> <span class="o">=</span> <span class="n">sample</span><span class="p">.</span><span class="n">dx_t</span>
</code></pre></div></div> <p>For a custom affine schedule, use <code class="language-plaintext highlighter-rouge">AffineProbPath</code> with a scheduler:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">flow_matching.path</span> <span class="kn">import</span> <span class="n">AffineProbPath</span>
<span class="kn">from</span> <span class="n">flow_matching.path.scheduler</span> <span class="kn">import</span> <span class="n">LinearVPScheduler</span>


<span class="n">path</span> <span class="o">=</span> <span class="nc">AffineProbPath</span><span class="p">(</span><span class="n">scheduler</span><span class="o">=</span><span class="nc">LinearVPScheduler</span><span class="p">())</span>
<span class="n">sample</span> <span class="o">=</span> <span class="n">path</span><span class="p">.</span><span class="nf">sample</span><span class="p">(</span><span class="n">x0</span><span class="p">,</span> <span class="n">x1</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>
</code></pre></div></div> <p>That mapping is the package version of the equations from Part 5.</p> <h2 id="training-objective">Training objective</h2> <p>The continuous training objective remains velocity regression:</p> \[\mathcal{L}(\theta)= \mathbb{E}\left[\|v_\theta(X_t,t)-\dot{X}_t\|_2^2\right].\] <p>In code, <code class="language-plaintext highlighter-rouge">sample.dx_t</code> is the target:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pred</span> <span class="o">=</span> <span class="nf">velocity_model</span><span class="p">(</span><span class="n">sample</span><span class="p">.</span><span class="n">x_t</span><span class="p">,</span> <span class="n">sample</span><span class="p">.</span><span class="n">t</span><span class="p">)</span>
<span class="n">loss</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">mean</span><span class="p">((</span><span class="n">pred</span> <span class="o">-</span> <span class="n">sample</span><span class="p">.</span><span class="n">dx_t</span><span class="p">)</span> <span class="o">**</span> <span class="mi">2</span><span class="p">)</span>
</code></pre></div></div> <h2 id="minimal-implementation">Minimal implementation</h2> <p>For <code class="language-plaintext highlighter-rouge">flow_matching==1.0.10</code>, the continuous solver API is <code class="language-plaintext highlighter-rouge">ODESolver.sample</code>. It accepts <code class="language-plaintext highlighter-rouge">x_init</code>, <code class="language-plaintext highlighter-rouge">step_size</code>, <code class="language-plaintext highlighter-rouge">method</code>, <code class="language-plaintext highlighter-rouge">time_grid</code>, and <code class="language-plaintext highlighter-rouge">return_intermediates</code>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>
<span class="kn">from</span> <span class="n">flow_matching.solver</span> <span class="kn">import</span> <span class="n">ODESolver</span>


<span class="n">solver</span> <span class="o">=</span> <span class="nc">ODESolver</span><span class="p">(</span><span class="n">velocity_model</span><span class="o">=</span><span class="n">velocity_model</span><span class="p">)</span>
<span class="n">time_grid</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">linspace</span><span class="p">(</span><span class="mf">0.0</span><span class="p">,</span> <span class="mf">1.0</span><span class="p">,</span> <span class="mi">33</span><span class="p">)</span>

<span class="n">samples</span> <span class="o">=</span> <span class="n">solver</span><span class="p">.</span><span class="nf">sample</span><span class="p">(</span>
    <span class="n">x_init</span><span class="o">=</span><span class="n">x0</span><span class="p">,</span>
    <span class="n">step_size</span><span class="o">=</span><span class="mf">1.0</span> <span class="o">/</span> <span class="mi">32</span><span class="p">,</span>
    <span class="n">method</span><span class="o">=</span><span class="sh">"</span><span class="s">midpoint</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">time_grid</span><span class="o">=</span><span class="n">time_grid</span><span class="p">,</span>
    <span class="n">return_intermediates</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <p>The velocity model can be a callable or a <code class="language-plaintext highlighter-rouge">ModelWrapper</code>. Extra keyword arguments passed to <code class="language-plaintext highlighter-rouge">sample</code> are forwarded to the velocity model, which is useful for conditional checks and guided settings.</p> <h2 id="code-result">Code result</h2> <p>The package bridge run uses <code class="language-plaintext highlighter-rouge">CondOTProbPath</code> to sample exact conditional path points, then uses <code class="language-plaintext highlighter-rouge">ODESolver</code> with the matching conditional velocity to integrate the same endpoints up to $t=0.98$. The figure separates the API wiring from the verified output. In the path panel, solid blue curves are solver trajectories and dashed orange curves are the exact path samples drawn on top of them.</p> <figure> <picture> <img src="/assets/img/blog/flow-matching-guide/flow-matching-official-package-bridge.svg" class="img-fluid rounded z-depth-1" width="1120" height="700" alt="Official flow_matching package check showing API wiring, dashed exact CondOTProbPath samples, solid ODESolver trajectories, and a verified solver-to-path residual panel." data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>The solver-to-path agreement metric compares the final solver state with the <code class="language-plaintext highlighter-rouge">CondOTProbPath</code> sample at the same final time, $t=0.98$. The residual inset magnifies only that tiny terminal gap. The displayed remaining distance to the endpoint is separate: it is the intentional path segment from $t=0.98$ to $t=1$, not solver error. The point of the run is API wiring: the path object, velocity callable, and solver agree on tensor shapes and time arguments.</p> <h2 id="sampling-procedure">Sampling procedure</h2> <p>For a trained continuous model, the package sampling pattern is direct:</p> <ol> <li>choose the source batch <code class="language-plaintext highlighter-rouge">x_init</code>;</li> <li>wrap or pass the velocity model;</li> <li>choose <code class="language-plaintext highlighter-rouge">time_grid</code>, <code class="language-plaintext highlighter-rouge">method</code>, and <code class="language-plaintext highlighter-rouge">step_size</code>;</li> <li>call <code class="language-plaintext highlighter-rouge">ODESolver.sample</code>.</li> </ol> <p>The conceptual checks from the earlier parts still apply. The solver method should match the field’s time behavior, the path scheduler should match training, and diagnostics should separate numerical integration error from model quality.</p> <p>The package also has a discrete branch, including <code class="language-plaintext highlighter-rouge">MixtureDiscreteProbPath</code>, <code class="language-plaintext highlighter-rouge">MixtureDiscreteEulerSolver</code>, and <code class="language-plaintext highlighter-rouge">MixturePathGeneralizedKL</code>. That is separate from the continuous 2D examples used in this series.</p> <h2 id="next-part">Next part</h2> <p>The series ends with an export checklist after all draft checks pass.</p> <h2 id="references-and-visual-resources">References and visual resources</h2> <ul> <li>Primary guide and codebase paper: <a href="https://arxiv.org/abs/2412.06264">Flow Matching Guide and Code</a>.</li> <li>Official package repository: <a href="https://github.com/facebookresearch/flow_matching"><code class="language-plaintext highlighter-rouge">facebookresearch/flow_matching</code></a>.</li> <li>Core paper: <a href="https://arxiv.org/abs/2210.02747">Flow Matching for Generative Modeling</a>.</li> </ul>]]></content><author><name>Quan Tran Hong</name></author><category term="tutorial"/><category term="flow-matching"/><category term="tutorial"/><category term="reading-notes"/><category term="generative-modeling"/><summary type="html"><![CDATA[Map toy code to the package.]]></summary></entry></feed>